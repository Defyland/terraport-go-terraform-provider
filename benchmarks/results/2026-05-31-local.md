# Local Benchmark Result - 2026-05-31

Command:

```sh
go test -run '^$' -bench='BenchmarkClient' -benchmem ./internal/bankport
```

Output summary:

```text
BenchmarkClientCreate100PartnerApps-10      100   11489944 ns/op   1077758 B/op   14019 allocs/op
BenchmarkClientRetry429Twice-10             314    4125915 ns/op     28598 B/op     330 allocs/op
```

Interpretation:

- Creating 100 fake partner apps is dominated by local HTTP server round trips and JSON encode/decode.
- Retry benchmark includes two 1ms backoffs per operation, so latency reflects intentional delay rather than CPU pressure.
- Provider-level `TestAccHundredPartnerAppsApply` gives Terraform lifecycle evidence; this benchmark isolates the HTTP client cost.
